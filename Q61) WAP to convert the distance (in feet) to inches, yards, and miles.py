length = float(input("Enter the length value to convert: "))

print(f"The length value in inches is {length * 12} unit, "
      f"in yards is {length * 0.333} unit, and in miles is {round(length * 0.000189394, 3)}")

